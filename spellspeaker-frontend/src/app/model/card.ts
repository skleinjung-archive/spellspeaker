export class Card {
  selected: boolean;

  name: string;
  baseCard: boolean;
  type: string;
  reusable: boolean;
  manaCost: number;
  castingTime: number;

  element: string;
  power: number;
  elementStrength: number;

  text: string;
}
